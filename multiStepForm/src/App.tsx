import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);
  Vite + React;
  return (
    <div className="App">
      <h1>Vite + React</h1>
    </div>
  );
}

export default App;
